## 运行

`init.sh` 初始化环境并配置systemd
`startup.sh` 运行

## 配置

参见 `init.sh`

在 `/root/.secrets`配置环境变量

```dotenv
# qq bot token
QQBOT_APP_ID=xxx
QQBOT_APP_SECRET=xxx
# deepseek ai token
DEEPSEEK_API_KEY=xxx
```
